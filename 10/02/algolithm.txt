//1. 画像の宣言 (入力画像，グレースケール画像，二値画像，一時的な画像，出力画像)

    //2. 輪郭の座標リストの宣言

    //3. 画像の入力 (カラー)

    //4. 入力画像を出力画像にコピー

    //5. グレースケール化

    //6. 二値化 (固定しきい値で実装．しきい値: 100)

    //7. 二値画像コピー

    //8. 輪郭抽出

    //9. 表示